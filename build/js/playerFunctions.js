// playerFunctions.js, where we define all the functions that can be taken on player objects

function player (hand, deck, discard) {
		this.hand = hand;
		this.deck = deck;
		this.discard = discard;

		// this.purchaseCards
}
